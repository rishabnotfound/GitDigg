name: Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  publish:
    name: Build and Publish
    runs-on: ubuntu-latest
    # Only run publish steps on pushes to main (not on PRs)
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.21"
          cache: true

      - name: Build binaries
        run: |
          mkdir -p dist

          platforms=("darwin/amd64" "darwin/arm64" "linux/amd64" "linux/arm64" "windows/amd64" "windows/arm64")

          for platform in "${platforms[@]}"; do
            GOOS="${platform%/*}"
            GOARCH="${platform#*/}"
            output="dist/gitdigg_${GOOS}_${GOARCH}"

            if [ "$GOOS" = "windows" ]; then
              output="${output}.exe"
            fi

            echo "Building $GOOS/$GOARCH..."
            CGO_ENABLED=0 GOOS=$GOOS GOARCH=$GOARCH go build -ldflags "-s -w" -o "$output" ./cmd/gitdigg
          done

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Prepare npm packages
        run: |
          # Use short commit SHA as version instead of GitHub tag
          VERSION="0.0.0-${GITHUB_SHA::8}"
          echo "Version: $VERSION"

          cd npm/gitdigg
          npm version "$VERSION" --no-git-tag-version --allow-same-version
          cd ../..

          declare -A PLATFORMS=(
            ["darwin-arm64"]="darwin_arm64"
            ["darwin-x64"]="darwin_amd64"
            ["linux-arm64"]="linux_arm64"
            ["linux-x64"]="linux_amd64"
            ["win32-arm64"]="windows_arm64"
            ["win32-x64"]="windows_amd64"
          )

          for npm_platform in "${!PLATFORMS[@]}"; do
            go_platform="${PLATFORMS[$npm_platform]}"
            pkg_dir="npm/@gitdigg/$npm_platform"

            mkdir -p "$pkg_dir/bin"

            if [[ "$npm_platform" == win32* ]]; then
              binary="gitdigg.exe"
              src="dist/gitdigg_${go_platform}.exe"
            else
              binary="gitdigg"
              src="dist/gitdigg_${go_platform}"
            fi

            if [ -f "$src" ]; then
              cp "$src" "$pkg_dir/bin/$binary"
              chmod +x "$pkg_dir/bin/$binary"
            fi

            os="${npm_platform%-*}"
            cpu="${npm_platform#*-}"

            cat > "$pkg_dir/package.json" <<EOF
          {
            "name": "@gitdigg/$npm_platform",
            "version": "$VERSION",
            "description": "GitDigg binary for $npm_platform",
            "repository": "https://github.com/rishabnotfound/GitDigg",
            "license": "MIT",
            "os": ["$os"],
            "cpu": ["$cpu"],
            "files": ["bin"]
          }
          EOF
          done

      - name: Publish platform packages
        run: |
          for dir in npm/@gitdigg/*/; do
            if [ -d "$dir" ]; then
              cd "$dir"
              npm publish --access public --tag latest || true
              cd -
            fi
          done
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish main package
        working-directory: npm/gitdigg
        run: npm publish --access public --tag latest
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}